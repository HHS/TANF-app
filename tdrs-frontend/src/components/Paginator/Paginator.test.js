import React from 'react'
import { render, fireEvent, waitFor } from '@testing-library/react'

describe('Paginator components', () => {
  describe('PaginatorArrowButton', () => {})

  describe('PaginatorPageNumberButton', () => {})

  describe('Paginator', () => {})
})
