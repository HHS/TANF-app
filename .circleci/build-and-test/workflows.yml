# workflows:
  build-and-test:
    when:
      or: 
        - << pipeline.parameters.build_and_test_backend >>
        - << pipeline.parameters.build_and_test_frontend >>
    jobs:
      - secrets-check
      - test-frontend:
          requires:
            - secrets-check
      - test-backend:
          requires:
            - secrets-check
      - test-e2e:
          requires:
            - secrets-check
