services:
  tdp-admin:
    stdin_open: true
    tty: true
    user: "0:0"
    build:
      context: .
      target: localdev
    command: sh -c "corepack prepare yarn@4.6.0 && corepack yarn dev --hostname 0.0.0.0 --port 3000"
    ports:
      - 3001:3000
    environment:
      - NEXT_PUBLIC_AUTH_URL=http://host.docker.internal:8989
      - NEXT_PUBLIC_AUTH_BROWSER_URL=http://localhost:8989
      - NEXT_PUBLIC_BACKEND_URL=http://host.docker.internal:8989/v1
      - ADMIN_BACKEND_URL=http://host.docker.internal:8989/admin-api/v1
      - ADMIN_FRONTEND_ORIGIN=http://localhost:3001
      - ADMIN_API_PROXY_TOKEN=local-admin-proxy-token
      - NEXT_PUBLIC_BACKEND_HOST=http://host.docker.internal:8989
    volumes:
      - ./:/home/node/app
      - /home/node/app/node_modules
      - /home/node/app/.next
    networks:
      - local

networks:
  local:
    driver: bridge
